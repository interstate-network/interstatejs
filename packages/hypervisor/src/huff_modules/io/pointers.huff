template <ptr>
#define macro LOAD = takes(0) returns(1) { <ptr> mload }

template <ptr>
#define macro STORE = takes(1) returns(0) { <ptr> mstore }

template <ptr>
#define macro CDLOAD = takes(0) returns(1) { <ptr> calldataload }